# reproduction

reproduction <- function(no_betas, betas, temp_mu, temp_var, pivotal_temp, 
                         N, t) {

  # for each potential mating function
  for (b in 1:no_betas) {
    
    beta <- betas[b]
    
    # for each temperature scenario 
    for (t in 1:length(temp_mu)) {
      
      
      
      
    }
    
    
  }
  
}